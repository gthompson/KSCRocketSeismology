# Archived code

`read_launchpads_from_kml.py` is retained only for provenance.

It defines the same `read_kml_points()` function as `modules/kml_utils.py`.
The active notebooks import `read_kml_points` only from `kml_utils.py`.
