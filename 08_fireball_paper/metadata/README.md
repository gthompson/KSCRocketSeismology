# Required metadata files

Copy the authoritative files into this directory using these names:

- `launchpads_cameras.kml`
- `KSC.xml`

These files were not among the six uploads used to build this package, so they are not included in the ZIP.
