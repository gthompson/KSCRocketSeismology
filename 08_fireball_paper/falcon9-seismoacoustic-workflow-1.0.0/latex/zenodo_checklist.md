# Release alignment update — manuscript revision 49

The supplement’s Zenodo section and this checklist now use the same release inventory: executable workflow; BCHH MiniSEED and StationXML with calibration provenance; the complete catalogue and review decisions; timing/weather/geometry inputs; event, energetics, transfer, and figure products; and the regional waveform/metadata/visual-review archive. The 93 flagged regional phase windows have been visually reviewed. Before release, record each decision in the machine-readable regional review table; do not leave all rows as `not_reviewed`.

A nearby-launch calibration comparison is not yet a release product because its raw-count MiniSEED extracts were unavailable during the latest notebook run. If retained in the manuscript, archive the extracts, StationXML, head-assignment metadata, and derived comparison output.

# Zenodo checklist — notebook-by-notebook review

Working checklist; update as each notebook is reviewed. Use portable, minimal inputs, not an SDS directory subset. Share common inputs between notebooks rather than duplicating them. Preserve original measurements; generated caches can be rebuilt. File paths below are relative to the workflow repository unless stated otherwise. Items are requirements/recommendations, not a claim that a release package has been assembled or tested.

## Current status after final 165/170 runs and manuscript version 43

Use scientific_issue_resolution_audit_43.md as the current issue-status authority; earlier checkpoint sections below retain historical review context. The final user runs of canonical 165 and 170 completed all substantive cells with no saved errors and match the reviewed source (ignoring empty cells). They supersede the earlier bounded execution descriptions for those notebooks. Manuscripts are now main43.tex and supplement43.tex.

Closed reporting items include descriptive phase counts, regression pressure/capture definitions, complete principal overlap-group exclusion/resampling, shared-data independence wording, model-equivalent energy/conditional shock language, generated-table definitions and stale table references. Still open: initial/principal physical pulse-to-PGV identification; calibration stability/over-range response; 48-h precursor input/review provenance; formal regional candidate review; exact historical slide evidence; final portable-release build/proofs/DOI. The deferred movie spectrogram/scrolling marker/map enhancement remains recorded in remaining_workflow.md.

## 000 — Correct BCHH instrument response

Current decision: retain the empirical calibration values and response processing pending the before/after launch comparisons. No notebook edit or rerun made in this review step. Adopted values are DD1 0.1234, DD2 1.4499, DD3 0.1000 Pa/count; manuscript reciprocals are rounded. Do not replace these with inferred nominal head sensitivities.

### Minimum reproducible inputs

| Material | Simplest deposit form | Current input / action |
|---|---|---|
| Six-channel raw accident waveform | One MiniSEED file containing DD1–DD3 and DHE/DHN/DHZ | `data/miniseed/01_bchh_raw_event_window.mseed`. Saved execution reports 2016-09-01 13:04:12.080–13:37:11.912 UTC, 250 Hz. No SDS archive needed for 000. |
| Original instrument response and channel metadata | One event-epoch StationXML containing the six channels | Current code reads `data/metadata/KSC.xml`; an event-only copy is generated as `data/outputs/000_correct_bchh_instrument_response/bchh_event_inventory_original.xml`. For a minimal release, put the verified event subset at the configured input path, or update that path. Keep original and empirically rescaled responses distinguishable. |
| Source coordinates | Existing small KML file, shared with subsequent notebooks | `data/metadata/launchpads_cameras.kml`. 000 requires its SLC40 point. Retaining this small file avoids changing the reader; do not introduce a second geometry representation unnecessarily. |
| Executable workflow | Notebook, imported project modules and environment specification | `notebooks/000_correct_bchh_instrument_response.ipynb`, `modules/project_config.py`, `modules/kml_utils.py`, their dependencies, and a tested dependency/environment file. Use repository-relative paths; SDS configuration is optional. |
| Calibration provenance | Short Markdown note; source scans/PDFs where available for distribution | Record exact factors, units, historical lift-test source, provisional uncertainty, unconfirmed head assignments and high-pressure response caveat. Identify recalled procedural details as recollections. Preserve the Sakurajima motivation. Historical notes/report extracts are supporting evidence, not waveform-processing prerequisites. |

### Useful release products (reproducible from the inputs)

- `BCHH_20160901_response_corrected.mseed`: one six-channel floating-point MiniSEED, with pressure in Pa and ground velocity in m/s documented externally.
- `BCHH_20160901_empirically_calibrated.xml`: response used to convert the raw counts. Clearly state that this is not a response to remove again from already corrected physical-unit waveforms.
- `BCHH_20160901_correction_summary.csv`: channel geometry, original/adopted sensitivities, units and processing summary.
- `bchh_response_processing.json`: parameters and provenance; make release paths portable while preserving original provenance separately if needed.

These four files are under `data/outputs/000_correct_bchh_instrument_response/`. Pickle is a generated workflow cache, not the sole archival representation. Later notebooks currently use it, so either allow 000 to regenerate it or adjust their readers before omitting it from a run-from-intermediates package. The diagnostic PDF can be regenerated and is optional as an archived convenience output.

### Follow-up checks before release

- Run 000 from a fresh environment using only the deposited MiniSEED, metadata and code, with no SDS access.
- Add concise calibration provenance to notebook narrative/metadata when preparing the release; preserve all current factors until evidence supports a change.
- Document that response shape is retained while scalar amplitude is rescaled; nominal poles/zeros are not independently validated by the small-signal lift factor.
- Check/report raw gaps and overlaps before merging. Current code interpolates during merge; this is a reproducibility safeguard to review, not evidence that the actual input contains gaps.
- Record the saved run's accepted 0.167-s leading boundary shortfall; it is well before the event and does not alone indicate a scientific problem.
- Resolve response-normalization frequency provenance: the code sets sensitivity frequency to 1 Hz, whereas the recalled lift test was about 0.5 Hz. This is a metadata/response-model consistency check, not grounds to change factors without examining the original response.

## Historical cross-notebook planning items (current status in version-43 issue audit)

- The approximately 33-minute file used by 000 cannot reproduce the 48-hour precursor screen. Identify and package its full six-channel input separately, preferably as one MiniSEED, when reviewing that analysis. Avoid redundant copies if a shared longer file can serve both workflows with explicit trimming.
- Before/after launch calibration comparison: one multichannel MiniSEED per selected launch, applicable StationXML epochs, a small event/channel metadata table and the comparison code/results. Exact launches and intervals remain to be selected by the user.
- Track manual picks, weather profiles, regional waveforms, timing measurements and figure-specific inputs when their notebooks are reviewed; do not assume Archive.zip alone contains these dependencies.

## 010 — Prepare canonical analysis inputs

Reviewed copy: `reviewed_notebooks/010_prepare_analysis_inputs.ipynb` in the review workspace. Original notebook preserved. Changes add actual coverage, physical units and exact median-filter metadata, correct the producer notebook name, and explicitly write FLOAT64 MiniSEED. All saved outputs were cleared; the edited notebook has passed syntax checks but has not been rerun. No calibration, timing, geometry or baseline algorithm was changed.

### Inputs and simplest archive forms

- Reuse 000's raw MiniSEED, original StationXML, KML and code; no new observational waveform input is needed for 010.
- For execution, 010 currently requires the corrected Pickle, calibrated StationXML and channel-summary CSV generated by 000. The Pickle is reproducible from portable inputs; it need not be the only or permanent archive waveform format.
- Include `modules/geometry_products.py`, `modules/xml_utils.py`, `modules/kml_utils.py` and shared configuration with the workflow source.
- Preserve the canonical five source times and labels in the small generated `key_events.csv`, with provenance/time convention documented. They are adopted source times, not measured arrivals; the CSV does not resolve catalogue-row or PGV mappings.

### Useful generated release products

All in `data/outputs/010_prepare_analysis_inputs/`:

- `bchh_corrected_moving_median_baseline_removed.mseed`: one six-channel waveform file, with baseline subtraction on pressure only. This is distinct from 000's response-corrected file; do not substitute it for analyses that intentionally use the pre-baseline data.
- `analysis_configuration.json`: baseline settings, channel units, references, geometry/product locations and requested/actual coverage. Portable release paths still need coordinated handling in downstream readers; current executable schema uses generated absolute paths.
- `bchh_channels.csv`: channel geometry and inherited calibration/processing metadata; `bchh_physical_sensors.csv` is a small derived geometry view for downstream use.
- `key_events.csv`: five adopted source-event definitions.
- `launchpad_camera_locations.csv`, `bchh_stations.csv` and copied inventory XML are regenerable views of shared inputs. Retain only where required for the chosen run-from-intermediates entry point; no additional field archive is needed.
- Both Pickle products are regenerable caches. The pre-baseline Pickle duplicates the waveform content available from 000, though it serves an existing downstream path.

### Scientific / release follow-ups

- No figure is generated in 010.
- Keep the 1-s moving median pending the energy-sensitivity analysis in 060/energetics; it is nonlinear. At 250 Hz the kernel is 251 samples spanning 1.000 s between endpoints (not an erroneous 1.004-s duration correction).
- Preserve DD2 as the time-reference convention when testing coordinate uncertainty later: 010 currently chooses the pressure sensor nearest the seismometer, so rerunning reference selection on perturbed coordinates could switch the reference. Perturbation analyses must retain or explicitly transform the reference convention.
- Geographic source distances and UTM coordinates are different geometric representations; later array analyses should document how they use each. 010 itself does not propagate positional uncertainty.
- Source-time provenance and physical-event/catalogue mapping remain tasks for the relevant downstream notebooks, not solved by validating the five event IDs here.

## 020 — KSC weather and effective acoustic speed

Required new observational input: the single small `data/metadata/nasa_weather_tower_data.xls` supplied by NASA/KSC. Keep its original headings and units, and record provider, acquisition/provenance, UTC convention, tower identifiers and coordinates if available. This one file is simpler than any larger meteorological archive and works with the existing reader. A CSV-only release is possible but needs an explicit loader change; the revised notebook exports `weather_observations_used.csv` as a portable normalized analysis subset, not a replacement input for the current code.

Reuse 010's geometry/configuration; no waveform file is needed by 020. Include `modules/weather_analysis.py`, `modules/physics.py`, shared configuration and dependencies (including xlrd for the XLS reader).

Useful derived files in `data/outputs/020_analyze_ksc_weather/`: `weather_observations_used.csv`, `weather_height_profile.csv`, `weather_time_profile.csv`, `weather_acoustic_summary.csv`, and `weather_processing.json`. The latter explicitly records weighting, bounds, sound-speed formula and lower-profile extension. Figures are reproducible; retain the two supplementary composite PDFs for convenient comparison to the paper. Do not duplicate input observations as multiple mandatory archive formats.

Review findings: 105 rows, seven times, three tower/measurement locations; 84 valid temperature and 84 humidity observations in the saved run. Temperature/RH means pool all available heights; the 0–65 m restriction applies to vector wind integration. The lowest valid wind profile level is about 40 m, so much of that integral uses constant downward extension. This assumption is now documented and shown in the wind-profile composite. It is not a measured near-ground profile. Distance is to the seismometer, not DD2.

Changes in reviewed copy: remove repeated saves to identical diagnostic-PDF paths; separate wind-rose legend from angular label; label frequency percentages; show constant lower-profile wind extension; export normalized observations and provenance. No sound-speed formula, wind calculation, event time, or adopted acoustic result has been changed. Saved notebook outputs cleared.

Outstanding scientific work: compare the simple humid-air sound-speed approximation to an appropriate reference model; assess representative-height/near-ground wind assumptions and temporal/spatial sampling. The observation standard deviations are not confidence intervals on the propagation model. Coordinate uncertainty belongs in the later geometry analysis. Do not silently substitute a new speed into downstream source times or shock estimates before reviewing its effect.

Validation for reviewed 020: all code cells executed successfully in the existing flovopy_asl environment with outputs redirected to `/tmp/review020_outputs`. Acoustic-summary, height-profile and time-profile CSVs match the original saved products. The edited wind composite was rendered and visually checked. Downloadable notebook deliberately retains cleared outputs; validation did not overwrite the repository products.

020 Cramer comparison revision: reviewed copies of physics.py and weather_analysis.py accompany the updated notebook. The shared approximation remains the default; Cramer is a separate function with explicit pressure (Pa), RH (percent) and CO2 (ppm). The weather summary accepts optional model arguments and records model/parameters. The comparison uses assumed 101325 Pa and 400 ppm, not claimed observations. Every notebook code cell executed successfully with reviewed modules and temporary outputs. At the observed mean conditions, Cramer increases effective speed by 0.287732 m/s and shortens the seismometer travel time by 0.003314 s. Archive the comparison CSV and bibliographic reference with these assumptions. No new weather measurement input is required. Direct verification against the original paper and field representativeness remain qualifications.

## 030 — Manual catalogue reconciliation

New required inputs: three small CSVs in the configured LEGACY_EXPORT_DIR: `manual_arrival_picks.csv`, `legacy_infrasound_event_catalogue.csv` and `legacy_catalog_157_events.csv`. Deposit these directly with original pick IDs, original channel labels, accepted/deleted flags and epoch seconds. No Antelope database or MATLAB installation is needed to execute 030 from these exports. Preserve a short extraction-provenance note identifying the Antelope arrivals, MATLAB consolidation script, time conventions and legacy catalogue definitions; original source files/script are useful provenance where distributable, rather than mandatory runtime dependencies.

Reuse 010 geometry/configuration and 020's adopted weather summary. 030 itself reads no waveform file. Its two targeted waveform decisions are historical, hard-coded review metadata: archive the review decision table and provenance. The shared accident MiniSEED enables later verification of those decisions; do not imply that 030 recomputes their correlation/SNR statistics.

Useful outputs: `final_153_event_catalogue.csv`, `reduced_time_pick_assignments.csv`, `event_catalogue_decisions.csv`, association-window sweep, `catalogue_identifier_mapping.csv`, `catalogue_processing.json`, `overview_phase_membership_audit.csv` and `overview_phase_counts_audit.csv`. The identifier mapping connects candidate numbers, retained numbers and manual pick IDs, but does not assign physical named-event PGV windows. Figures are diagnostic provenance and can be regenerated.

Reviewed changes: use reconstructed rather than surveyed geometry wording; repair outdated notebook/output references; guard targeted decisions against a >5-ms mismatch from their reviewed event times; add explicit candidate/final-ID mapping, provenance and the overview phase audit. Original association algorithm, 0.040-s window, calibration, adopted weather and retain/exclude decisions are preserved. Saved outputs cleared in the downloadable copy.

Outstanding: decide whether historical phases mean exhaustive event clusters or descriptive plotting intervals. The audit records the current intervals without changing them. Physical event complexes for regression require their measurement windows, and are deferred to Notebook 100. Stable time guards protect manual decisions if upstream results change; a guard failure requires renewed review, not simply increasing its tolerance.

## 040 — InfraPy Bartlett validation

No new observational input: reuse 010's baseline-corrected MiniSEED (the preferred portable reader input), geometry/configuration, 030's final catalogue and 020's adopted weather summary. No SDS subset or additional waveform export is needed. Pin the InfraPy version/commit and its dependencies; the notebook records version and module location, but a local module path alone is not a reproducible software identifier.

Useful outputs: event-centred validation CSV, fine-grid and coarse-grid background-window CSVs, background threshold grid/tail audit, waveform-quality and parameter-sensitivity tables, continuous beam-window CSV, candidate/recovery/review tables, and JSON provenance. Continuous beam windows are a compact reproducible diagnostic, not raw observational data. Retain the publication/supplementary PDFs used in the paper for convenience.

Reviewed changes: verify nonoverlap of sampled background windows, document limits on independence and empirical false-alarm interpretation, add matched coarse-grid background calculations and upper-tail/leave-one-out threshold diagnostics, and export background time coverage/sample duration and scope in provenance. The original fine-grid threshold remains in use for the continuous detector and peak recovery, so this revision does not silently change published recovery counts. The added coarse-grid audit informs whether a later threshold change is warranted.

Outstanding: assess threshold sensitivity results before adopting a grid-matched continuous threshold; full continuous recovery changes would require rerunning the scan and updating all dependent tables/captions. With 153 background windows, the 99.5th percentile remains an unstable extreme-tail estimate; leave-one-out ranges are sensitivity diagnostics, not confidence intervals. This scan uses the packaged accident record and does not reproduce the 48-h visual precursor screen. Background observations may share temporal dependence despite nonoverlap. The three-sensor geometrical systematic uncertainty remains for the later array analysis.

040 validation: executed cells through event/background validation and the new coarse-grid/tail audit in infrapy_env. Full continuous scan and later figure cells were not rerun. All revised cells pass syntax checks. Inspection of the saved continuous figure identified a colorbar overlapping the direction/velocity panels; the revised code assigns a separate margin axis. This figure-layout change still needs visual confirmation after rerunning the notebook.

## 050 — Catalogue waveform review

No new observational data. Reuse the shared six-channel corrected accident waveform (000/010), 010's baseline-corrected pressure MiniSEED, geometry/configuration, 020's adopted weather summary, and 030's catalogue, pick assignments and decision CSVs. Optional quality measurements come from 040's `event_waveform_quality.csv`. The current reader uses the corrected six-channel Pickle cache for seismic review; regenerate this from the shared MiniSEED and metadata through 000/010 rather than making Pickle a separate mandatory observational deposit. No per-event waveform files or SDS subset are needed.

Useful derived outputs: `final_event_figure_index.csv`, `final_event_review_metrics.csv`, `excluded_candidate_audit.csv` and `event_review_provenance.json`. The paired PDF gallery (306 retained figures plus two excluded-candidate figures) is optional reproducible review material, not a mandatory raw-data collection. Deposit PDFs only if useful to document manual review; retain the index with the corresponding directories. HTML is an optional link index for PDFs.

Reviewed changes: validate exact assigned pick IDs, unique reconstructed IDs and baseline channels; validate optional quality-table event IDs and, when available, event reference times; enforce travel-time correction agreement without relative tolerance; fix obsolete notebook/file references, duplicate optional input path and PNG-named switches that actually save PDFs; replace invalid PDF image embeds with PDF links; record export settings and measurement scope in provenance. Catalogue decisions, physical scaling, adopted weather, windows and stack calculations remain unchanged. Legacy `s03_` metric columns remain compatible.

Interpretation: short review-window stack peaks are not complete-pulse amplitudes or energies. Mean/median stacks differ from medians of individual sensor peak measurements. Closely spaced entries may represent one physical explosion complex. Standardized SNR/correlation/lag values are imported from 040, not independent checks computed here; historical excluded-candidate statistics remain historical decision metadata.

Validation: all code cells executed in flovopy_asl with outputs redirected to temporary directories and batch PDF writing disabled. All 153 original metric rows/columns match the existing output within numerical tolerance; the current upstream quality table additionally supplies `review_reference_epoch_s`. Both retained preview figures were rendered and inspected. Exact pick audit confirms 369 retained picks and one excluded candidate. The final added reference-time guard checks available reference times; the upstream table supplies a value for only one event (difference 0.24 microseconds; tolerance 1 microsecond), so it does not establish reference-time agreement for the other 152 events. The downloadable notebook has outputs cleared and normal PDF export enabled. The full PDF batch and optional HTML branch were not executed.

## 060 — Moving-median baseline validation

No new observational waveform input: reuse 000/010's corrected six-channel waveform and canonical baseline-removed MiniSEED, 010 geometry/configuration, 020 adopted weather summary, 030 catalogue/pick assignments and optional 040/050 quality/review CSVs. The corrected six-channel Pickle is a regenerated metadata-bearing cache, not an additional mandatory observational deposit. If an optional `selected_filter_test_events.csv` is used, include that small selection CSV and its identifier convention; otherwise the deterministic ranking is documented in the notebook and exported selection table.

Useful derived products: exact baseline-product verification CSV/JSON, selected-event IDs/ranks, pulse measurements, cross-channel consistency, corrected window-sensitivity and paired-comparison tables, `principal_baseline_energy_sensitivity.csv` and `baseline_sensitivity_processing.json`. The latter records full-trace processing order, adopted/test windows, principal-source key, timing speeds and fixed signal bounds. Full-trace baselines can be regenerated; do not deposit a separate waveform file for every test window or event. Diagnostic PDFs are optional, with publication-relevant sensitivity figures useful for comparison.

Scientific correction: the original sensitivity routine applied moving medians to 1.4-s event extracts (including a 2-s candidate kernel) and filtered the raw median stack separately. The reviewed routine computes each baseline on complete channel traces, then extracts/aligns/stacks, matching canonical processing order. This changes sensitivity outputs, while leaving the adopted correction, selected-event ranking and pulse algorithm unchanged. The 1-s candidate reproduces canonical selected measurements exactly. Verification now requires exact seismic preservation and tight FLOAT64 pressure agreement; all six channels match exactly. A centered nonlinear operation preserves timestamps, but can change selected extrema or threshold-crossing times; narration now makes that distinction.

Energy diagnostic: fixed principal signal offsets −0.70 to +1.05 s relative to predicted DD2 arrival are an explicitly declared exploratory reuse of Notebook 110's manually reviewed signal interval (extraction offset −1.45 s plus signal bounds 0.75–2.50 s). The source time comes from config's `lower_stage`, rather than a catalogue-row identity. The source-to-DD2 reduction uses 351 m/s; channel differential delays use the adopted weather summary. Exported actual sample endpoints document discretization. This adds no observational input or new calibration and is not a replacement for later 110/120 energetics. The fixed-window pressure-squared ratios equal energy ratios only with unchanged density, speed, geometry and other processing; the later local-baseline convention still needs verification.

Measured principal integral ratios relative to 1 s: 0.125 s gives 0.248–0.258; 0.25 s gives 0.444–0.481; 0.5 s gives 0.740–0.751; 2 s gives 1.087–1.130. This demonstrates baseline-dependent energy, rather than establishing an optimal window or an uncertainty interval. The default pulse-pair routine fails for reconstructed entry 13 on all three sensors and the stack. That failure reflects absence of an allowed positive-before-negative pair under the prescribed search/lag constraints, not proof of signal absence or a bad baseline. The high-quality selected subset does not establish sensitivity for the entire catalogue.

Validation: executed substantive measurement/sensitivity/verification cells in flovopy_asl with outputs redirected to `/tmp/review060_outputs`; skipped the three batch event-PDF loops (37/43/52). Preview diagnostic and new principal sensitivity figures were rendered and inspected. Adopted 1-s sensitivity matches canonical selected measurements. A subsequent addition exports paired-valid peak ratios and time changes; it was validated separately from saved sensitivity products. All downloadable notebook code passes syntax checks and saved outputs are cleared. The notebook does not rewrite canonical waveforms or change TeX results. Outstanding: verify principal window/local-baseline equivalence in later named-event analysis, evaluate quieter/broader events, and carry the revised sensitivity findings into the supplement after rerunning 060.

## 070 — Final-event waveform products

No new observational inputs. Reuse 010's canonical baseline-corrected six-channel waveform/configuration/geometry, 020's adopted weather summary and 030's final catalogue plus pick assignments. The current metadata-bearing reader and downstream 080 use Pickle caches; generate these from the shared MiniSEED and provenance rather than requiring another raw-data deposit. There is no need to deposit 153 individual event MiniSEED files or an SDS subset. The approximately 1.1-s event Pickles are generated analysis caches required by the present downstream implementation, not independent observations; package them only for execution convenience.

Useful small derived products: `final_event_waveform_products.csv`, `candidate_event_pick_assignments.csv`, `candidate_event_stream_index.csv`, `candidate_event_processing_metadata.json`, and the new `event_segment_sample_coverage.csv` and `excluded_candidate_pick_assignments.csv`. `candidate_airwave_event_catalogue.csv` is a compatibility alias of the final waveform-product table, not another scientific catalogue. The legacy candidate-number alias denotes final retained event numbers. `unassociated_accepted_infrasound_picks.csv` retains its existing meaning and filename; the two associated picks from the rejected candidate are now explicitly preserved separately. Diagnostic event PDFs are optional.

Reviewed changes: validate unique catalogue/pick identifiers, exact retained pick/channel support and spans, a single weather-summary row, unique geometry/stream channels, finite waveform values and agreement between geometry/weather delays and authoritative pick corrections. Add actual sample endpoints, sample counts, rates and physical units for all 918 channel segments. Record observed-UTC segment timing, assigned-channel stack/SNR scope and short-window limitations in processing metadata. Fix obsolete notebook references and PDF switch naming; diagnostic figures identify stack channels, use authoritative association-channel labels and share a pressure scale. Pulse measurement, classification, adopted alignment speed, saved segment windows and compatibility filenames remain unchanged. No phase boundaries are defined or altered by 070.

Interpretation: assigned-channel morphology SNR is different from 040 standardized fixed-window quality metrics and 060's three-channel diagnostics. Noise-return boundary conventions also differ between 060 and 070; their durations should not be treated as identical measurements. Saved segments preserve observed receiver UTC; diagnostic pressure arrays alone are differentially aligned. Approximately 1.1-s coverage does not establish complete seismic-coda or physical-event capture. Nearby segments can overlap and represent one physical explosion complex. Morphology labels never determine catalogue membership or authoritative later quality selections.

Validation: all notebook code cells executed in flovopy_asl with output redirected to `/tmp/review070_outputs` and the batch diagnostic-PDF switch disabled. All 153 original table rows and original columns agree after normalizing CSV serialization (the generated segment directory necessarily changes). Every channel in every saved event Pickle matches the canonical source samples exactly. There are 369 retained associated picks, 90 two-channel and 63 three-channel events, and two separately exported excluded-candidate associated picks. Morphology counts remain 45 robust, 89 moderate, 18 weak and one complex/noncanonical. The edited preview figure was rendered and inspected. The batch PDF gallery was not rerun. Original repository files remain untouched; the downloadable copy has cleared saved outputs and PDF export enabled. No numerical manuscript update is warranted by this notebook.

## 080 — Planar array propagation

No new observational data. Reuse 010 geometry/configuration, 020's adopted weather summary, 070's event-table and generated unshifted six-channel segment caches, and 040 Bartlett table/metadata for the combined figure. Those event Pickles are reproducible caches from the shared accident MiniSEED; neither 153 event MiniSEED files nor an SDS subset is required. Ensure the shared `project_config.py` accompanies the release: it supplies the only overview phase definitions. If overriding the geometry or event-product paths, retain the actual selected inputs in provenance.

Useful outputs: `planar_array_results.csv`, `planar_window_stability.csv`, pairwise diagnostics, principal representative result, combined planar/Bartlett results, and review provenance. Compressed coarse/fine score surfaces and composites are useful inversion diagnostics but reproducible rather than raw observations. Keep publication PDFs for comparison; the per-event PDF gallery is optional. Added `principal_conditional_geometry_sensitivity.csv` and its figure/provenance are small geometry-model diagnostics and require no additional measurement file.

Reviewed changes: remove 080's hard-coded phase-date fallback and require ordered, nonoverlapping `config.OVERVIEW_PHASE_INTERVALS`; use the manuscript's explicit 351 m/s speed for source-UTC figure reduction, while retaining the original weather speed as a comparison line. Validate a single weather row, finite coordinates and the DD2 reference expected by this implementation. Remove duplicate Bartlett input candidates. Label relative-direction error bars as scoring-window dispersion excluding geometry uncertainty. Add deterministic fixed-delay principal-solution sensitivity to 1/2-m single-sensor offsets in eight directions. Nominal searches, morphology eligibility, principal representative row 13 and A/B/C thresholds are unchanged. The combined figure's source times change by a sub-millisecond amount from using exactly 351 rather than 350.976933 m/s; receiver/reference times and nominal inversions do not change.

Geometry diagnostic interpretation: hold DD2 coordinates and nominally inferred DD1/DD3 relative delays fixed, perturb one of DD1/DD3, then solve the two-component slowness vector. At 1 m, sampled back-azimuth changes span −2.090 to +2.036 degrees and speed changes −11.835 to +11.868 m/s. At 2 m they span −4.232 to +4.123 degrees and −23.637 to +23.768 m/s. These are conditional sampled scenarios, not confidence intervals or worst-case bounds. They omit simultaneous/correlated sensor errors, source/receiver-height and propagation-model errors, delay-estimation noise and complete waveform re-inversion. Small scoring-window dispersion cannot establish comparably small total propagation uncertainty. Fine-grid near-maximum widths may be truncated at a search edge. Verify projected-grid versus geographic-north conventions before interpreting small systematic direction offsets.

Validation: imports/load/geometry/eligibility/search functions executed in flovopy_asl using the reviewed 070 temporary products. Reran the principal row's full coarse/fine search and nine scoring-window perturbations; its direction 200.3 degrees, speed 352.5 m/s, coherence 0.9984 and window dispersions reproduce the original values. Executed the new geometry diagnostic and rendered its figure. Executed the combined 153-event figure using saved original nominal planar results and current 040 Bartlett outputs; confirmed phases equal config and visually checked layout. The full 153-event inversion, every per-event figure, and all composite/summary cells were not rerun. Original saved A/B/C counts are 51/45/57; all 96 A/B rows have nine valid window perturbations and none has a fine-grid boundary optimum. The downloadable notebook has cleared outputs. No nominal numerical TeX change is warranted yet; geometry sensitivity supports strengthening the supplement's uncertainty discussion after the full rerun.

Outstanding: full rerun and checkpoint consistency (leave resume disabled for a fresh release run); principal row/physical-complex mapping in later named-event analysis; coordinate reconstruction provenance and correlated error models; full waveform re-inversion for perturbed geometry if converting these scenarios into a quantitative uncertainty claim. Shared phase definitions are now mandatory and are not modified in 080.

## 090 — Finite-distance propagation sensitivity

No new observational files. Reuse 010 geometry/configuration and canonical source coordinates, 070 generated unshifted event caches, and 080 complete nominal planar results/quality classes/scoring windows. The single shared accident MiniSEED and metadata regenerate the caches; do not create another SDS subset or mandatory per-event waveform collection. No phase boundaries are defined or modified in 090.

Useful derived products: fixed-source speed results, range-profile results, merged planar/finite-distance comparison, principal representative result, `circular_geometry_audit.csv`, `circular_analysis_summary.json`, and the new `direction_reference_audit.json` and `matched_direction_curvature_audit.csv`. Compressed speed/profile surfaces are optional but useful to inspect the open range ridge and reproduce composite figures without rerunning inversions; they are not additional observations. Retain the supplementary comparison PDF used in the paper. The disabled legacy Cartesian source-grid experiment need not supply redundant release products.

Reviewed changes: require every trial to use all samples of the inherited scoring window, rather than silently dropping uncovered columns; use the same endpoint convention as 080. Validate DD2 reference, unique geometry rows, finite coordinates and paired source-coordinate overrides. Guard entirely nonfinite search surfaces. Remove duplicate supplementary PDF save. Preserve a single unsuffixed inherited quality/planar/fixed-source coherence column when merging profile results. Clarify seismometer versus centroid references and grid versus geographic north. Add input, source-elevation, grid and interpretation provenance; distinguish the joint-score deficit from Bartlett coherence. Fixed-source searches, source coordinates, assumed 0-m source elevation, profile grids and inherited quality classes remain unchanged.

Audit results: centroid bearing is 199.024952 degrees from projected grid north and 199.229422 degrees from geographic north (difference +0.204470 degrees). The configured geographic reference is the BCHH seismometer, at 199.438026 degrees, rather than DD2. Separate north convention and reference-point changes before interpreting small systematic azimuth offsets. At matched centroid direction, the exact-minus-planar relative-delay differences at 351 m/s are −0.275 ms for DD1 and −0.178 ms for DD3. This isolates nominal curvature from the additional direction constraint imposed by fixing SLC-40; the roughly +4 m/s fixed-source minus freely directed planar speed difference is not a pure curvature measurement. Reported versus projected-distance residuals also depend on projection/elevation conventions and are not independently established coordinate errors.

Validation: executed input/geometry helpers and all 153 fixed-SLC-40 speed searches in flovopy_asl with temporary output; original speeds, scores and relative delays match. Reran principal row 13's two full range/cross-range profiles; numeric profile results match the original. Executed principal and composite/summary figure code, using saved original profile surfaces/results for the 153-event supplementary composite. The edited supplementary comparison was rendered and visually checked. An additional envelope check confirms all 153 segments cover every delay on both configured range-profile grids. The full 153-event range-profile inversion and the disabled Cartesian source-grid experiment were not rerun. Downloadable notebook outputs cleared; original files untouched.

Scientific status: source range remains unbounded on the sampled score-drop profile for all 153 original events; these spans are fixed-speed diagnostics, not confidence regions or joint source/speed constraints. Geometry/height/model uncertainty is not incorporated. Principal row 13 remains a representative catalogue entry rather than a proven mapping of the complete physical complex. A common north convention across 040/080/090 and manuscript comparison lines should be resolved before interpreting sub-degree offsets. The original nominal propagation results are preserved; the new reference/curvature audit merits a later supplement clarification rather than an immediate change to inferred source location or reported energy.

## Executed checkpoint — Archive3.zip (060–090)

All substantive cells of the four reviewed notebooks were executed with no saved errors; source differences from our reviewed copies are blank lines only. This supersedes earlier statements that the full 080 inversion, full 090 range-profile search and corresponding batch figures had not been rerun. Execution confirms the original 51/45/57 planar quality classes, unchanged nominal propagation summaries and open range profiles. It does not close later named-event, regression-dependence or absolute-calibration questions.

Important interpretation update for release metadata/manuscript: use paired-valid 060 peak comparisons rather than treating unpaired median differences as a common pressure scaling. For the same 23 valid stack pulses, median peak-to-peak ratios at 0.25/0.5/2 s relative to 1 s are 0.987/1.001/1.003. The separately fixed principal pressure-squared ratios remain 0.740–0.751 at 0.5 s and 1.087–1.130 at 2 s; these are conditional processing sensitivities, not energy confidence intervals. Archive both tables and the full-trace processing order/window provenance.

Additional 080 correction after this rerun: nominal azimuth columns are grid-north angles; the geographic seismometer-reference comparison line is now converted to grid north. Local meridian convergence is 0.204510 degrees, giving a comparison line of 199.233516 degrees. The updated notebook records native CRS/angle convention, convergence, geographic input bearing and converted comparison bearing in `planar_review_provenance.json`. Regenerate this JSON and affected reference-line figures using the revised notebook; the corrected combined PDF is supplied at this checkpoint. This does not alter nominal waveform inversion values. Include `project_config.py`, EPSG:32617 and explicit centroid/seismometer/DD2 reference descriptions. No phase boundaries are redefined.

No additional observational data file is needed. Keep the executed notebook archive, compact audit CSV/JSON outputs and corrected publication figure; event waveform Pickles and NPZ surfaces remain reproducible convenience products. Repository originals and the remote Zenodo deposit are unchanged.

## 100 — Event amplitudes, coupling and selected-complex energy

No additional observational files. Reuse the single accident MiniSEED, calibration/response and geometry metadata, shared configuration, 020 weather summary, 070 generated event caches and 080 planar results. The response-corrected continuous stream and moving-median stream are reproducible products of that same record; do not require another SDS subset or 153 standalone event recordings.

Retain the executed notebook, `event_acoustic_seismic_amplitudes.csv`, regression summary/sensitivity/leave-one-out tables, transfer estimates and weighting/strong-event sensitivities, and disjoint-complex/cumulative energy tables with energetics metadata. New compact provenance products are `pressure_pgv_measurement_overlap_audit.csv`, `pressure_pgv_regression_selected_rows.csv`, `pressure_pgv_regression_bootstrap_slopes.csv` and `named_event_pressure_pgv_mapping_audit.csv`. The continuous transfer table now records the shared phase definition, configured source/arrival support, nonoverlapping segment count and unused tail samples. Keep the publication PDFs; the optional 153-event waveform gallery is reproducible and need not be a mandatory release product.

Review: preserve nominal measurements; fail incomplete seismic/event measurements; distinguish actual regression-window overlap from energy-complex union windows. Exclude all selected rows in the principal overlap group and add group resampling. Phase boundaries come exclusively from `config.OVERVIEW_PHASE_INTERVALS`. Record named nearest-arrival matches without asserting a manually verified physical pulse association. Clarify pointwise conditional H1 ranges/null thresholds, shared-data dependence and alternative radiation scenarios.

Results: original 153 pressure/PGV/capture measurements reproduce. Full descriptive regression remains n=46, slope 0.962230, R² 0.952557. Those rows occupy 45 measurement overlap groups. Excluding principal group 13/14/15 removes selected rows 13 and 14: n=44, slope 0.943381, R² 0.904944. Group-bootstrap 2.5–97.5 percentiles are 0.871091–1.011254; this is a conditional sensitivity, not total physical uncertainty. Named-event PGVs require comparison with 110 before manuscript changes; legacy event-H1 row resampling still ignores overlapping-record dependence. Remote Zenodo unchanged.

Validation: executed every code cell of the final revised notebook in flovopy_asl with temporary output; disabled only the optional 153-event diagnostic gallery. All 153 original pressure/PGV/capture measurements and the numerical event/continuous H1, disjoint-complex energy and cumulative-energy tables reproduce. The revised regression figure was rendered and visually checked. Outputs cleared in the downloadable notebook; repository originals untouched.

## 110 — Named-event pressure windows

No new observational files. Reuse the shared accident MiniSEED, response/calibration and geometry metadata, `project_config.py` canonical source times and 010 analysis configuration. The full-trace moving-median pressure Stream is a reproducible cache, not another required raw waveform collection. The optional catalogue association audit additionally uses 100's amplitude table. No phase definitions are introduced or modified.

Retain `key_event_pressure_measurements.csv`, `key_event_measurement_windows.csv` and `key_event_measurement_metadata.json`. New compact diagnostics: `key_event_channel_sample_and_baseline_audit.csv`, `key_event_catalogue_pgv_overlap_audit.csv` when 100 exists, and `key_event_window_review.pdf`. The audit records actual snapped sample grids, absolute peak epochs, baseline/signal counts and support, edge flags, and fixed-support pressure-squared integrals before/after residual constant subtraction. Include `modules/event_measurements.py`, which defines the original trimming, baseline and median-summary conventions. No mandatory per-event MiniSEED files or SDS subset.

Review/validation: title changed to 110, all original cells retained, complete-input/window guards and two diagnostic cells added. Every code cell executed with temporary outputs; all 16 original pressure rows reproduce, and the new window figure was rendered and visually checked. Outputs cleared for download; repository originals and remote Zenodo unchanged.

Scientific status: MEDIAN means median independently measured sensor extrema, not extrema of a median waveform. Initial named support overlaps catalogue rows 1–3, principal support overlaps 11–15; physical pulse-to-PGV assignment remains unresolved. Principal local-baseline pressure-squared ratios are 1.0188/1.0109/1.0166 by DD1/DD2/DD3, but effects are larger for some small signals. These are processing sensitivities, not total energy uncertainty. Check consistency of 120's waveform correction/support before updating energies or TeX. Absolute calibration and bracketing-launch checks remain outstanding.

## 124 — Acoustic energetics and ground-motion exposure

No new observational files. Reuse the shared accident MiniSEED, calibration/response metadata, canonical geometry/configuration, adopted weather and 110 named windows/metadata. Full-trace baseline-corrected waveform products remain reproducible caches; no additional SDS subset or per-event recording set is required. Include shared `project_config.py`, `event_measurements.py` and `physics.py` with the release.

Retain named channel/summary energetics and metadata, ZRT exposure, continuous 5-s bins and exact interval totals, weak-shock summary/profile and publication figures. New `named_pressure_window_comparison_110_124.csv` records the deliberate sensor-specific timing/local-baseline differences from 110. Channel energetics now record sampled signal support/counts and absolute extrema times. Continuous totals include per-channel phase fractions for additive accounting. Keep the legacy NB120 output directory for downstream compatibility, with producer metadata correctly identifying notebook 124.

Review: preserve original results and appropriate shared axes; title fixed to 124. Remove redundant payload-start override in favor of mandatory revised 110 input; require finite complete waveforms/windows and canonical source-time/product consistency. Shared phases remain unchanged. Clarify per-channel shifted versus unshifted named windows, nonadditive interval medians, ZRT seismometer timing, passband differences and conditional uncertainty/timing interpretations.

Validation: all revised code cells executed; original named acoustic energetics, ZRT exposure, continuous bins/interval totals and weak-shock summary reproduce. SPL/energy summary rendered and visually checked. Outputs cleared for download; originals and remote Zenodo unchanged. No immediate nominal TeX change. Named-PGV physical association, absolute calibration/bracketing launches and total energy uncertainty remain outstanding.

## 130 — Seismic rotation, polarization and spectrograms

No new observational recording is required. Reuse the single accident MiniSEED, calibrated StationXML with channel orientation and response metadata, shared configuration/geometry and adopted weather summary. The baseline-corrected input and rotated `seismic_zne.pkl`/`seismic_zrt.pkl` Streams are reproducible caches; retain them as optional convenience products for 140, not mandatory additional raw files or an SDS subset.

Useful derived products: `polarization_timeseries.csv` (NPZ is a convenience duplicate), `polarization_summary.json`, `multiband/multiband_polarization_timeseries.csv`, publication polarization/context figures and both spectrogram resolutions. The summary now records exact filter/window conventions, geographic/source-relative axial angles, source-to-seismometer timing, no-interpolation policy, per-component display normalization, common spectrogram amplitude scale, software versions/TwistPy source path and requested/effective multiband settings. Record the actual commit of any local TwistPy checkout in the release environment. Compact CSV and JSON products plus the executed notebook are sufficient; spectrogram arrays can be regenerated from the shared record.

Review/validation: all original cells/analyses preserved; reject gaps/nonfinite waveforms, verify rotation amplitude preservation, correct stale notebook references, and use common Z/R/T colour limits in dB re 1 m/s magnitude spectrum. Executed every code cell; original broad-band and multiband numerical polarization results reproduce. All figure batches generated; revised 0.4-s spectrogram rendered and visually checked. Outputs cleared for download. No phase boundaries changed, no nominal TeX numerical change, and remote Zenodo unchanged.

Outstanding interpretation: axial polarization is not unique wave-type/direction evidence; high DOP can occur in weak or filter-edge data; 90%-overlapping windows are dependent. The 1-s low-frequency window poorly resolves behavior at 0.5 Hz. Per-component normalized traces cannot establish component amplitude ratios; magnitude spectrum is not PSD and zero-padding does not improve window resolution. Preserve StationXML orientation provenance and address direct-wave/causal-onset claims in 140.

## 140 — Focused direct-seismic-wave search

No additional observational file. Reuse the single accident MiniSEED, calibrated response/orientation StationXML, shared source times/geometry, adopted weather, and 130's regenerated unfiltered ZRT cache. Optional stacking uses the complete 100 or 080 catalogue for times, quality and neighboring-event gaps. Rotated/event Pickles are reproducible products; no new SDS subset or per-event MiniSEED collection is required.

Retain onset candidates/band summaries, trailing polarization, confirmation/support summary, frequency-dependent envelope-delay proxy tables, direct-search metadata and publication diagnostics. For the corrected optional stack retain `isolated_stack_events.csv` with reduced source epochs, `isolated_event_stack.csv` and its figure. The summary now documents source-to-seismometer reference, catalogue DD2-arrival reduction, trailing analytic support, gap policy and empirical thresholds/controls. Include exact ObsPy/SciPy versions and upstream response-correction settings in release environment metadata.

Review/validation: all original analyses retained. Correct optional arrival-as-source stack bug (same seven events, origins earlier by 4.012624 s); confine polarization Hilbert transforms to each trailing window; require at least 10% Z and R energy for the descriptive quadrature flag; remove correlation-to-degree mapping from the phase figure. Add complete waveform/padding/Nyquist and configuration checks. Every final code cell executed and figure batches generated; phase-discrimination figure rendered and visually checked. Original amplitude-onset and group-delay tables and real-covariance polarization reproduce. Quadrature candidate counts intentionally change; rerun and regenerate those tables/figures and corrected stack. Download outputs cleared, originals and remote Zenodo unchanged. No phases redefined.

Outstanding: robust-MAD thresholds are not false-alarm probabilities, overlapping windows are dependent, local Hilbert transforms retain finite-window edge effects, and a causal filter stage does not make upstream response correction causal. Legacy onset-bound columns represent confirmation-window support, not physical onset confidence bounds. Adopted acoustic source origins, site response, zero-phase backward leakage and nonunique polarization prevent automatic P/S/Rayleigh/Love identification. No direct-wave speed or phase claim is newly established. Check any TeX use of old stack timing/quadrature flags after the rerun.

## 141 — Generated supplement event table

No new observations. Include the revised generator, authoritative 100 amplitude CSV, generated `latex/generated/supplement_event_measurements.tex` and its small JSON hash/count/convention record. Optional regeneration uses `--source`/`--output`; corrected repository default matches the supplement's input path. Executed generation of 153 rows/46 regression entries; no measured results changed. Full manuscript recompilation was not performed at this step.

## 145 — Regional waveforms and detectability audit

This notebook needs additional regional observational records beyond the BCHH accident file. Use one MiniSEED per selected station/channel covering the audited event and equal-duration pre/post controls, including necessary filter padding; no SDS archive is needed. Preserve raw cached records, including a failed/incomplete record if needed to explain an availability result. Pair each with event-epoch response/orientation StationXML and a station/channel manifest with provider, exact request bounds, coordinates, channel priorities, sample rates and actual coverage. Freeze the event-time discovery inventory/query log and availability/fallback/failure table; do not substitute the modern network.

DWPF metadata correction: `IU.DWPF.30.LDO.xml` contains an InstrumentPolynomial valid from 2010-09-24, with input Pa, output counts and coefficients [0, 0.1]. ObsPy converts counts directly to Pa. This is valid conversion metadata despite absent ordinary sensitivity/stages. Include this original StationXML; no replacement calibration file is required. Frequency-domain response prefilter limits do not apply to this direct-polynomial branch.

Derived products: complete phase-screening metrics, preserved manual-review and timestamped prior-review files, station/supplement audit, run settings/effective bands, focused candidate records/plots, post-hoc moveout/leave-one-out and pseudo-origin tables with explicit selection caveats, corridor record section, BCHH band comparison and guarded amplitude/period worksheet. An executed notebook and environment/response-prefilter provenance are necessary. Avoid duplicating raw regional observations as multiple processed MiniSEED products.

Review: actual sampled peak timing, complete waveform/control coverage, pascal-response validation, screening restricted below the response-prefilter taper, preservation of manual review, fully covered pseudo-origin packets, adopted-source-clock wording and disabled unverified/unconfirmed period-yield inference. Low-rate effective bands and timestamps can change; rerun before TeX updates. Automated candidates, celerity-bin labels and post-hoc percentiles are not confirmed signals, propagation paths or formal false-alarm probabilities.

Validation was offline and bounded: code compilation, cached calibrated screening at three northern stations, validation of DWPF polynomial response, and review-rerun preservation using saved tables. No new FDSN discovery/download or complete figure/analysis rerun. Originals and remote Zenodo unchanged. Outstanding manual association and selection/propagation uncertainty prevent new regional detection/non-detection/yield claims.

Correction following DWPF metadata inspection: absent sensitivity/stages does not mean absent calibration. The InstrumentPolynomial was already present and is accepted by revised 145. Static conversion is 0.1 Pa/count; it does not itself establish a flat dynamic response over the full nominal screening band.

141 notebook conversion: `141_generate_supplement_event_table.ipynb` preserves all revised generator checks and output formatting, with editable path overrides and correct notebook producer metadata. Every notebook code cell executed; generated LaTeX is byte-identical to the revised script output. Release the notebook as the preferred generator; the Python script is optional. No new observational files.


## Archive4 executed checkpoint (100–145; 14 September 2026)

This full executed checkpoint supersedes the earlier bounded/offline validation description for 145. All seven notebooks executed without saved errors; the archive hash and reconciliation are recorded in checkpoint_100_145_review.md. Release the final notebook sources and environment/version information, not only screenshots or notebook outputs.

- Retain 100's overlap-group membership, resampling configuration/seed and row/group bootstrap summaries, plus measurement-window/SNR/capture provenance. Groups derive from actual sampled intervals across the catalogue, including unselected bridges; preserve this definition separately from energy complexes.
- Include 110/124 support/baseline comparison audit and named-event configuration. Keep catalogue IDs and explicit physical pulse-to-PGV assignment status. Channel-specific energy contributions and continuous phase fractions are needed to reproduce accounting; scalar medians alone are insufficient.
- Include 130 orientation/rotation, bands/windows, software versions and normalization provenance; 140 source-reduced stack origins, causal-filter settings, finite trailing-window Hilbert implementation and amplitude/component-energy gates. Flags and window support must not be labelled validated phases or onset confidence intervals.
- Include generated/supplement_event_measurements.tex and its JSON source hash, alongside the canonical 153-row measurement CSV and notebook 141.
- Regional inventory: retain 171-target discovery/query/failure log (165 usable, six failed), 509 phase metrics, 93 candidate rows and review classifications/notes. All 93 candidates have now been visually reviewed. The released machine-readable table must record the completed decision and notes for each row; preserve the review evidence.
- Retain event-epoch StationXML, including DWPF's existing InstrumentPolynomial [0, 0.1] (0.1 Pa/count), and response-branch/sample-rate/filter-band provenance. No replacement DWPF calibration is needed. Direct polynomial conversion does not apply the frequency-domain response prefilter.
- Use one MiniSEED file per selected regional station/channel with sufficient event, matched-control, packet and filter-padding coverage, plus StationXML; avoid a partial SDS archive. Record exact deposited bounds and distinguish selected packet examples from the broader search inventory.
- Include regional geometry, selected peak sample times, corridor station list, pseudo-origin settings/full-coverage eligibility and timing-fit tables. Post-hoc pseudo-p is descriptive; period–yield calculations remain disabled while the relation and association are unverified.
- Deposit final main/supplement sources and bibliography with current figure assets and relative paths. Main42 retains the ten-item main-paper limit. Verify a clean portable execution/build from the deposit before release. No remote Zenodo changes have been made.


## Remaining notebooks 150–170 / Archive5

Reviewed copies and workflow roles are documented in notebook150_170_review.md. Keep one canonical 165; the suffix-1 variant was consolidated, not a separate analysis. 160 remains the animated movie producer. This checkpoint uses earlier saved executions plus bounded validation against today's current upstream outputs; it does not replace a complete release rerun.

- Reuse the canonical calibrated BCHH MiniSEED/StationXML, geometry, weather summary and YAML/config already listed; no additional SDS subset or duplicate observational waveform deposit is required. Include both observational/input-response provenance and derived 010/130 cache generation instructions.
- Video reproduction requires source citation/URL, attribution/permission scope, exact local source-file hash, native video/audio stream start PTS/time bases/rates, anchor movie PTS and constructed UTC mapping. Provide portable external-file acquisition/path instructions. Choose movie/frame/audio deposit scope according to the actual permissions; article-figure permission does not by itself establish full-source-movie redistribution permission.
- Include 150 manual arrival/video pick JSON and final trial/identification notes, observed receiver picks, source-reduction speeds/distances, channel residuals, selected frame PTS/subframe settings and anchor uncertainty assumptions. Preserve revision history/backups for consequential manual changes; omit redundant caches when canonical records plus provenance reproduce them.
- Include canonical 165 frame/window audit CSV and provenance JSON, editable frame-selection controls and final permitted publication figure. It uses sensor-relative display windows and local baselines; retain that distinction from 110 measurements. Contact sheets are generated review aids, not a new required raw-data deposit.
- Include 160 final sync configuration, render/input-hash manifest, camera propagation audit, snapped/requested start times and quantization offset, alignment mode, filters/normalization/display flags and software versions (PyAV, ffmpeg, ObsPy, matplotlib). PNG frame caches and extracted WAVs are derived products; regenerate rather than requiring all frames. Include the final synchronized movie only within the applicable release permissions.
- Include 170 figure-generation provenance, final figure assets, named-event peak-alignment and multimodal frame audits, audio native-PTS/source provenance and configured overview phases. Static graphics can be generated without completing 160. Record map CRS, station/source coordinates and basemap source/license; support an offline map fallback or appropriately distributable assets for reproducibility.
- Deferred enhancement recorded: movie main panel + calibrated spectrogram with scrolling time marker + source/camera/BCHH map. Preserve one declared time basis, spectrogram scaling/window/bands and map provenance when implemented. No new observations are needed for that display enhancement.
- Remote Zenodo and repository originals remain unchanged. Full production movie, interactive manual picks and a clean portable release run remain author/release tasks.


## Version-43 final reconciliation / additional compact release products

- Include main43.tex, supplement43.tex and the bibliography with current canonical 165/170 figure assets. Preserve the main nine-figure/one-table limit. No new instrument observations were needed for the wording reconciliation. A corrected 145 map is included as a regenerated presentation asset.
- Include scientific_issue_resolution_audit_43.md and final_notebook_audit.json so completed checks and open evidence tasks are distinguishable. This is not a submission-readiness certification.
- New regression_capture_threshold_audit.csv and its reproducible audit script record the stricter 0.80/0.90/0.95 capture selection calculation from existing 100 measurements. Add source hash and numerical settings. These are conditional selection sensitivities, not replacement waveform measurements or new confidence intervals.
- New named_event_table_provenance_audit.csv/JSON record the physical label/configured source time, separate 110 pressure windows, representative Table-1 PGV rows 2/13/22/23, nearest-arrival rows 1/14/22/23, sampled seismic support/peak epoch, source hashes and unverified physical assignment status. Preserve this provisional status until author pulse review is completed.
- Retain actual final 165 frame/window audit/provenance and 170 figure-generation/multimodal frame/peak-alignment audits from the real workflow, not temporary validation provenance. Native selected-frame offsets (+0.017, +0.520, -0.878, +0.089 s relative to adopted event origins) are visual selections, not four independently measured origin times.
- Supply the six-channel 48-h precursor input as a single MiniSEED, or one shared longer file with explicit accident trimming, plus metadata epochs, coverage/processing and visual-review notes. The existing 33-minute accident MiniSEED cannot reproduce the precursor claim. Do not invent a calibrated threshold from that visual result.
- Freeze the completed 145 visual-review decisions for all 93 automatic candidates. No new regional association or period-yield estimate follows from final publication plotting.
- Archive exact AGU slide/report evidence if a specific historical no-shock conclusion is attributed. The public 2017 abstract supports chronology/dominant event, not that statement; version43 limits the attribution accordingly.
- No extra raw observations are required for the capture/provenance audits. Reuse existing canonical 100/110 CSVs and shared configuration. Final page proofing, submission formatting and the DOI remain release tasks. Repository originals and remote Zenodo are unchanged.

- Version43 regional-map correction: include revised 145, corrected `supplement_regional_station_map_best_candidates.pdf`, offline `regenerate_145_map.py`, and compact input-hash/label provenance JSON. Reuse the existing availability CSV, phase metrics CSV and run metadata; no waveform retrieval or new raw deposit is required. Only map layout changed.

## 180 — Assemble and audit the Zenodo release

`notebooks/180_assemble_zenodo_release.ipynb` implements this checklist as a declarative, minimal release profile. It copies canonical notebooks/modules, shared observational inputs, compact CSV/JSON/XML/MiniSEED products, selected publication PDFs, and the necessary regional MiniSEED/StationXML collection. It excludes Pickles, NPZ search surfaces, per-event figure galleries, rendered movie-frame caches, and permission-dependent video/audio by default.

The notebook builds through a temporary directory, refuses accidental replacement, generates CSV/JSON manifests with SHA-256 checksums, writes a human-readable readiness report, optionally creates a ZIP, and verifies every staged file. A full temporary test selected 725 files (278.9 MB) and verified every checksum. It correctly reports three required files that are not currently present: the six-channel 48-h precursor MiniSEED, the calibration-provenance note, and the consequential manual video/event timing-pick JSON. Open scientific evidence, regional manual-review status, redistribution permission, final DOI metadata, and portable rerun/page-proof tasks remain explicit author actions rather than being treated as files that the assembler can invent.
